Role: Graphify Normalization Architect for ContextBridge

Objective:
Inspect this repository’s existing Graphify data and normalize it for ContextBridge compatibility without damaging, replacing, or overwriting the current Graphify outputs unless explicitly required.

Context:
This project may already contain Graphify-generated files, but they may not follow the structure or coverage ContextBridge expects.

Your job is to:
1. Inspect the repository structure first.
2. Inspect all existing Graphify folders and files.
3. Determine what is already usable by ContextBridge.
4. Identify what is missing, weak, inconsistent, or in the wrong structure.
5. Create only additive Graphify-compatible outputs where possible.
6. Preserve existing Graphify data unless a change is explicitly necessary.

Important rule:
Do not assume folder names such as `main_ui`, `main_service`, `src`, `client`, `server`, `frontend`, or `backend`.

You must first inspect the repo and infer:
- frontend/UI source roots
- backend/service/API source roots
- shared/core/common roots
- module/vertical roots
- Graphify roots

If these boundaries are unclear after inspection, stop and ask the user for:
- frontend root(s)
- backend root(s)
- shared/core root(s)
- Graphify root(s)

What ContextBridge expects:
1. A central Graphify root such as:
   graphify-out/

2. Business/flow packs under module folders such as:
   graphify-out/<module>/<pack>/behavior/
   graphify-out/<module>/<pack>/raw/source-files.txt
   graphify-out/<module>/<pack>/raw/graph.json

3. Folder-level ownership graphs near source areas where relevant, such as:
   <source-area>/graphify-out/graph.json

CRITICAL — single source of truth per file:
A file that belongs to both a business/flow pack (item 2) and a folder-level ownership
graph (item 3) ends up with a symbol-level node list in more than one graph.json. When
you add or complete nodes for such a file, derive its node list ONCE and copy the same
node/edge entries identically into every graph.json that references this file -- do not
independently re-derive it per location. If you fix or add a node for a file in one
graph.json, apply the identical fix to every other graph.json that also references that
file, in the same pass, so the copies never drift apart from each other.

Normalization rules:
- Do not assume the existing Graphify structure is sufficient.
- Inspect actual folders and files first.
- Reuse existing Graphify data wherever possible.
- Do not overwrite existing Graphify outputs unless necessary.
- Prefer additive outputs such as:
  - enrichment/dependency-edges.enrichment.json
  - missing raw/source-files.txt
  - missing raw/graph.json
  - missing behavior docs
  - missing folder-level ownership graphify-out folders
  - missing behavior/retrieval-hints.md when the right files already exist but
    the pack does not bind real bug-report wording to the true owners
  - missing cross-layer edges when a UI call-site file and its matching backend
    controller/route file both exist in the pack but graph.json has no edge
    connecting them (see cross-layer edge rule below)

Your tasks:
1. Discover all Graphify-related folders in the repository.
2. Discover the actual code/source roots in the repository.
3. Classify Graphify artifacts into:
   - central business packs
   - module/submodule packs
   - folder-level ownership graphs
   - unrelated or non-usable Graphify artifacts
4. Compare the existing Graphify structure against ContextBridge needs.
5. Report:
   - what is already usable
   - what is missing
   - what should be added
   - what should be ignored
6. Then create the missing/additive Graphify outputs in the correct locations.

Required output format:
1. Repo source-root understanding
2. Existing Graphify inventory
3. Usable for ContextBridge
4. Missing for ContextBridge
5. Additive files/folders to create
6. Risks or assumptions
7. Final implementation summary

Important constraints:
- Do not delete existing Graphify data.
- Do not replace existing files unless explicitly required.
- Prefer additive normalization.
- If existing Graphify data is structurally different but still useful, adapt around it instead of forcing a rewrite.
- If ownership graphs are missing, create them.
- If business packs are missing source-files.txt or raw/graph.json, add them.
- While inspecting an existing raw/graph.json or ownership graph.json, sample at least
  one covered file per pack: count the top-level functions/const-functions/arrow-functions/
  classes actually declared in the real source file and compare against the nodes
  graph.json has for that file. Files can be silently inconsistent -- some declarations
  captured, others of the same shape skipped, with no error. Do not try to fix this
  yourself here; note which files show a mismatch so a dedicated symbol-completeness
  pass (prompt 1.4 in this same folder) can be run on them afterward.
- If the same file is referenced from more than one graph.json (a pack-level graph and
  a module/ownership-level graph both covering it), note whether their node lists agree.
  A symbol present in one copy but missing from another is the same kind of gap and
  should be listed the same way.
- When creating or completing source-files.txt, include ALL layers of the stack:
    controller, service, entity/model, DTO, UI page/component.
  Service files are the most commonly omitted and the most important for CB routing.
  A pack missing its service file means CB cannot route to the root cause.
- Also include runtime and bridge ownership when relevant to the flow:
    job/scheduler, resolver, policy, middleware, filter, bridge/handoff, router/action-panel,
    print/export controller/API, and downstream posting/runtime consumer files.
  A pack that contains only setup/config files but not the runtime consumer path is incomplete
  for ContextBridge debugging.
- If cross-file dependency edges are weak, add enrichment files instead of editing original graph outputs first.
- Cross-layer UI-to-backend edge check: when a pack (or a folder-level ownership graph)
  contains both a frontend API-call file and the backend controller/route file it calls,
  verify graph.json actually has an edge between them, matched by route path + HTTP verb
  -- not just because both files happen to be listed in the same source-files.txt. Files
  being co-located in a pack does not mean ContextBridge can traverse from one to the
  other; only a real graph edge does. If the edge is missing, add it additively via
  `enrichment/dependency-edges.enrichment.json` (relation such as `calls` or
  `invokes_endpoint`) rather than editing the original graph.json. Apply this check to
  every pack you normalize, not only ones where retrieval already looked weak.
- If the right files are present but the pack still favors symptom files over owner files,
  add an additive retrieval-hints behavior file that explicitly maps:
  - real owner files
  - runtime owner files
  - bridge/handoff files
  - real-world business/symptom query terms
- Ensure the primary owner files named in `behavior/retrieval-hints.md` stay aligned
  with `raw/source-files.txt`. Do not describe one owner path in retrieval hints while
  `source-files.txt` is centered on a different owner path.
- Always add retrieval-hints for shell/view/runtime owner patterns when those
  files exist but are not likely to win from raw source-files alone:
  - admin configuration / audit / permissions pages for operational modules
  - dashboard shell/layout/global-search entry
  - legacy shell / overlay / connection-state UI
  - drawer/runtime/payment-side POS UI
  - workspace context/layout/view owners
  - startup/bootstrap/app-entry/runtime provider owners
  - reports/analytics/audit pages
  - receipt-template designer/list/editor pages
  - organization/store/terminal settings pages
  - investigation file preview/highlight/delete APIs
  - service catalog / pricing / billable charge APIs
- If a pack represents admin operations or runtime configuration, the retrieval
  hints must name the admin API/page/permission owners for that pack. Do not let
  retrieval hints drift toward sibling runtime/workflow owners that are not the
  pack's real primary path.
- Also include symptom wording that may omit the top-level module name entirely,
  such as `event images` + `linked order details`, `treatment master` + `staff availability`,
  or `report guard` + `exception filter`.
- Cross-pack backend ownership check: when a pack's workflow ends in a state change owned
  by a shared/central service that actually lives in a different pack (e.g. a charge,
  pricing, or ledger-posting service defined under a "catalog"/"pricing"/"billing" pack
  but genuinely invoked by this pack's own workflow), do not assume the workflow pack is
  self-contained. Confirm whether that shared owner file is present in this pack's own
  `raw/source-files.txt`. If it is missing, add it here as an additive fix even though its
  primary home is a different pack -- a workflow's true decisive owner can legitimately
  belong to more than one pack's file list. Flag this explicitly as a missing-file gap,
  not a wording gap.
  CRITICAL -- a `raw/source-files.txt` line alone is NOT a sufficient fix for this case.
  ContextBridge's ranking gives real scoring weight to a file's actual graph presence
  (`raw/corpus/` + nodes/edges in `raw/graph.json`), not to a plain text mention -- a
  file listed only in `source-files.txt` stays effectively invisible to ranking even
  though it is technically "in" the pack. When this check finds a missing shared owner,
  the fix must run that file through the same real AST/graph extraction this pack's other
  files went through (so it gets its own class/method nodes in `raw/graph.json` and a
  copy in `raw/corpus/`), not just an added line in `source-files.txt`. Only fall back to
  a text-only listing if real extraction genuinely cannot be run, and say so explicitly
  when reporting the fix.
- Explicit-decisive-owner check: when a pack's `behavior/retrieval-hints.md` lists several
  plausible owner files that could all reasonably answer a similar-sounding query (e.g.
  several UI modal files, or a generic ledger/forms service next to the one file that
  actually performs the specific action), do not just list them side by side. Name which
  one is THE decisive owner for the pack's core symptom, and say briefly why (e.g. "X
  posts the actual charge; Y and Z only read/display it"). A hint file that lists five
  plausible files with no distinction between them is a weaker fix than one that lists the
  same five files but marks the real decisive owner.

Normalization decision rule:
- If the miss is "file absent from pack", run the file through real graph extraction
  (`raw/corpus/` + `raw/graph.json` nodes/edges) the same way the pack's other files
  were extracted -- do not just add a `raw/source-files.txt` line and call it fixed.
  A text-only mention does not carry real scoring weight in ContextBridge's ranking.
  Only add a `raw/source-files.txt`-only line as a last resort if real extraction
  genuinely cannot be run, and say so explicitly when reporting the fix.
- If the miss is "owner present but not discoverable from real bug wording", fix behavior docs with additive retrieval hints.
- If the miss is "cross-module path missing explicit runtime edge", add enrichment rather than replacing original graph outputs.
- If the miss is "UI call-site file and backend controller/route file both present in the pack but no edge connects them in graph.json", add the missing edge via enrichment rather than replacing original graph outputs.
- If the miss is "file is in the right pack/structure but graph.json is missing some of
  its own declarations as nodes", do not try to fix it inline here -- flag the file and
  run the dedicated symbol-completeness prompt (1.4 in this same folder) on it.

Operational safety rules:
- Exclude non-source and polluting inputs from Graphify normalization unless the user explicitly asks for them:
  - test files and test folders such as `__tests__`, `*.test.*`, `*.spec.*`
  - vendor/build outputs such as `node_modules`, `dist`, `build`, `bin`, `obj`
  - nested Graphify artifacts such as `graphify-out`, backup Graphify folders, and temporary Graphify build folders
- Treat backup or temporary Graphify folders such as `graphify-out_pre_*`, `graphify-out_live_before_*`, and `_graphify_*` as non-indexable noise for ContextBridge purposes. Do not graph them and do not leave them inside the effective scan set for a rebuild.
- When normalizing or rebuilding an existing Graphify area, prefer a safe rebuild pattern:
  1. preserve the current live Graphify output as backup
  2. build the normalized output in an isolated temporary folder OUTSIDE the pack's own
     `raw/` directory entirely (e.g. a sibling temp path, not a subfolder nested inside
     `raw/`) -- a temp build folder created INSIDE `raw/` is still inside ContextBridge's
     scan path even though it feels "isolated," and a folder literally named `graphify-out`
     nested inside `raw/` will get indexed as if it were a second, separate pack. Real
     diagnosed case: a rebuild wrote its output to `raw/graphify-out/` instead of a true
     external temp location, and that leftover nested folder silently double-counted every
     file it covered in real search results because nothing in step 4 ever removed it.
  3. validate that required outputs exist such as `graph.json`, `GRAPH_REPORT.md`, `graph.html`, and `manifest.json` when applicable
  4. swap the normalized output into the live location only after validation succeeds, THEN
     delete the temporary build folder from step 2 -- do not leave it behind "just in case."
     A leftover temp build folder with its own `graph.json`/`manifest.json` inside the scan
     path is functionally identical to a duplicate pack and will get indexed.
- If Graphify fails because docs, images, or papers require semantic extraction and no API key is available, do not overwrite the live output. Use a code-only fallback when appropriate and report that semantic doc/image extraction was excluded.
- After any normalization or rebuild that changes Graphify outputs, explicitly remind the user that ContextBridge must be reindexed or synced before CB can use the updated data.

Success criteria:
- ContextBridge should be able to discover the normalized Graphify structure.
- Central packs should support business/workflow understanding.
- Folder-level ownership graphs should support owner-file and dependency tracing.
- Existing Graphify investment should be preserved as much as possible.
