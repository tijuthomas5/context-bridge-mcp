__pycache__/
*.py[cod]
.fuse_hidden*
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/
archive/
Questions/

# Visual Studio IDE local cache/index — machine-specific, never publish.
.vs/

# Generated local index and runtime telemetry — all data/ files are machine-specific.
data/
usage/
usage/*.jsonl
usage/dashboard_stats.json
usage/dashboard_stats.js
usage/rules_cache_reset.sentinel

#  docs — not for public distribution (old location — folder no longer exists)
READ_ME_Second/

# ─── PRIVATE / PROJECT-SPECIFIC — never publish ──────────────────────────
# Real runtime configs (ship the *.example.json templates instead).
config.json
config.hybrid.json
config.semantic.json
# Project-specific ranking profiles + rules (the swappable domain plugin).
# Catch-all: any *_profile.py / *_rules.json except the shipped example template.
rules/projects/*_profile.py
rules/projects/*_rules.json
!rules/projects/example_profile.py
!rules/projects/example_rules.json
# ─────────────────────────────────────────────────────────────────────────

# ─── DEVELOPER ARTIFACTS — not needed by downloaders, not published ──────
# Runtime analysis cache (regenerated per machine; holds local project data).
cache/
# All test data and scripts are machine/project-specific — excluded entirely.
# This folder is local-only; nothing under tests/ is checked in.
tests/
scripts/backup_graphify_out.ps1
Benchmark*/
scripts/smoke_test_*.py
scripts/e2e_analysis_test*.py
scripts/run_analysis_tmp.py
scripts/probe_search_context.py
scripts/smoke_test_analysis.py
scripts/smoke_test_hybrid_fusion.py
scripts/generate_real_benchmark_300.py
change_port.bat
run_full_quality_suite.bat
start_mcp_server.bat
start_mcp_server_hybrid.bat

start_Context_Bridge_DEBUG_LOCAL.bat
evals/
archive/artifacts/
archive/manual-tests/
# ─────────────────────────────────────────────────────────────────────────

# Local logs/temp files.
*.log
*.tmp
*.bak

# Temporary — not yet stabilized, keep local-only for now.
docs/AI_AGENT_GUIDE.md
codebase_atlas/

# Local/manual verification scripts for this workspace.
test_code_deep.py
test_code_loc.py
test_frontend_edge.py
test_verification.py
test_verification_fixed.py

# ─── repo_sync — real scan output is project-specific, ship *.example.json only ──
project_map.json
orphan_review.json
!project_map.example.json
# ─────────────────────────────────────────────────────────────────────────

# Local runtime / benchmark / manual review artifacts that often contain
# project-specific prompts, file names, audit notes, or machine-specific paths.
usage/*.md
usage/*.json
usage/test/
Questions/
Start_CB_Test_Mode.bat
